# Student Management System - Performance Test Config
# Copy to .env and set values (do not commit .env)

# Required: Backend API base URL (no trailing slash)
BASE_URL=http://localhost:4000

# Optional: JWT for authenticated endpoints (health works without it)
# Get via: curl -s -X POST $BASE_URL/api/auth/login \
#   -H "Content-Type: application/json" \
#   -d '{"email":"platform@example.com","password":"your-password"}' | jq -r '.token'
AUTH_TOKEN=
