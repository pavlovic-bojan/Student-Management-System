# Student Management System - Performance Test Config
# Copy to .env and set values (do not commit .env)

# Required: Backend origin (no trailing slash)
# Local: http://localhost:4000
# Prod:  https://student-management-system-backend-gwy8.onrender.com
BASE_URL=http://localhost:4000

# Optional: JWT for authenticated endpoints (health works without it)
# Seed user: platform-admin@sms.edu / seed-platform-admin-change-me
# Get via: curl -s -X POST $BASE_URL/api/auth/login \
#   -H "Content-Type: application/json" \
#   -d '{"email":"platform-admin@sms.edu","password":"seed-platform-admin-change-me"}' | jq -r '.token'
AUTH_TOKEN=
